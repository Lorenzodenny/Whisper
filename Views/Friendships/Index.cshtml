@model IEnumerable<Whisper.Models.Friendships>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Users.Username)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Users1.Username)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Users.Username)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Users1.Username)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FriendshipId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FriendshipId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FriendshipId })
        </td>
    </tr>
}

</table>
