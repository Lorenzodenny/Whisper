@model IEnumerable<Whisper.Models.Aphorisms>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Frase)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Frase)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AphorismId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AphorismId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AphorismId })
        </td>
    </tr>
}

</table>
